<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chaos Engineering Simulation</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <style>
        .info-box {
            margin-top: 20px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mt-5">Chaos Engineering Simulation</h1>
        <form action="/analyze" method="POST">
            <div class="form-group">
                <label for="url">Target URL:</label>
                <input type="url" class="form-control" id="url" name="url" placeholder="Enter target URL" required>
            </div>
            <div class="form-group">
                <label for="chaosType">Select Chaos Type:</label>
                <select class="form-control" id="chaosType" name="chaosType" onchange="showChaosInfo()" required>
                    <option value="">Select a chaos type</option>
                    <option value="latency">Latency</option>
                    <option value="abort">Abort</option>
                    <option value="slowRead">Slow Read</option>
                    <option value="injectError">Inject Error</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Analyze</button>
        </form>
        <div id="chaosInfo" class="info-box" style="display:none;">
            <h3 id="chaos-type-title"> Information</h3>
            <p id="chaosDescription"></p>
            <h4>Expected Output</h4>
            <pre id="chaosOutput"></pre>
        </div>
    </div>
    <script type="text/javascript" src="/js/index.js"></script>
</body>
</html>
